name: Swift

on: [push, pull_request]
  
jobs:
  build:

    runs-on: macos-latest
    strategy:
        matrix:
          destination: ['platform=iOS Simulator,OS=13.3,name=iPhone 11']
    

    steps:
    - uses: actions/checkout@v2

    - name: Run lint
      uses: ./
      id: hello
      with:
        who-to-greet: Mona the Octocat'
        
    - name: Run tests
      run: xcodebuild clean test -scheme wallabag -destination "${destination}" CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ONLY_ACTIVE_ARCH=NO | xcpretty
      env: 
         destination: ${{ matrix.destination }}
